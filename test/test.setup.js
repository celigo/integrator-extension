var should = require('should');
var assert = require('assert');

describe('JSONExtractor tests', function() {

  it('should check preProcessConcrete for different inputs', function(done) {

    console.log('test');
    done();
  });
});
